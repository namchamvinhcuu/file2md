.claude/
.gemini/

# CLAUDE.md / AGENTS.md = symlink per-máy tới shared claude-agents repo → không commit
CLAUDE.md
AGENTS.md

# Per-machine symlinks (vault, skill vaults) — không commit, tạo lại bằng harness setup script
.obsidian-vault
.python-skill
.common-skill

# Code-intelligence artifacts per-máy — Serena marker + graphify graph (rebuild bằng harness script)
.serena/
graphify-out/

# Python
.venv/
__pycache__/
*.pyc

# Secrets (API key LLM) — dùng .env.example làm mẫu
.env

# OS
.DS_Store
.vault-portal.conf
